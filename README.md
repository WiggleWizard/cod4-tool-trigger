CoD4 Trigger
=================

Executes a process/cli application when a threshold of players are on a specific server.

# Uses
Most use cases are autonomous reasons, for instance sending mail to someone when the server is full (remotely) or starting a DoS for instance when the server gets half full on the target system.
